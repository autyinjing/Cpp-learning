第十七章：标准库特殊设施

知识点积累：

1. string的下标编号习惯与bitset恰好相反：string中下标最大的字符（最右字符）用来初始化bitset中的低位（下标为0的二进制位）；

2. 一个给定的随机数发生器一直会生成相同的随机数序列。一个函数如果定义了局部的随机数发生器，应该将其（包括引擎和分布对象）
        定义为static的；

C++11新特性：tuple、bitset的all操作、随机数引擎和分布、hexfloat、defaultfloat
